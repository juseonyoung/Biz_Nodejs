extends layout

block content 
  //- tag=변수명 :text방식으로 문자열 표시
  //- #{변수명} :text방식으로 문자열표시
  //- !{변수명} :문자열중간에 tag등이 있으면
  h1 !{title}
  p Welcome to !{title}
  ul
    li
      a(href="/home") 홈으로
    li
      a(href="/home/my") 마이페이지
    li
      a(href="/home/input?nation=korea") query
    li
      a(href="/home/input/korea") params

  form(method="POST", action="/home/input")
    input(type="text",name="m_user")
    button 전송

